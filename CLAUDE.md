- Use `wiki/` only to document changes that are essential for understanding this
  repo correctly at a glance — i.e. changes whose omission could lead to a
  different result. Most edits don't qualify: skip the wiki update unless the
  change is genuinely important (a non-obvious decision, a gotcha, a behavior
  change someone would need to know about to not get it wrong).
- Start with [wiki/index.md](wiki/index.md).
