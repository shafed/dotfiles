# Java/Swing assumes a reparenting WM; without this, Swing windows (e.g.
# Scilab GUI) render as blank panes on Hyprland. hl.env in the Hyprland
# config covers its own children; this covers units started by systemd --user
# (app launchers, .desktop entries).
_JAVA_AWT_WM_NONREPARENTING=1
