# Dedicated QAT config for the nvim scratch editor.
# Inherits the normal kitty.conf/theme; only scratch-specific panel behavior
# belongs here so theme generation stays single-source.
edge center
background_opacity 1.0
