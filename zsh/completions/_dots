#compdef dots ds

# DOTFILES is exported before Oh My Zsh is sourced in zsh/zshrc.
# Generate command names and aliases from the CLI's single metadata source.
if [[ -x "$DOTFILES/dots" ]]; then
  eval "$("$DOTFILES/dots" completion zsh)"
  _dots_impl "$@"
else
  return 1
fi
